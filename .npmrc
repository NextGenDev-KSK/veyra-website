# The React Three Fiber ecosystem lags three.js peer ranges: postprocessing@6.35
# still pins `three < 0.167` while the project uses three@0.167 (which works fine
# in practice). Accept the peer mismatch so `npm ci` in CI matches local installs.
legacy-peer-deps=true
